
>> ## کلید های ترکیبی 

| عملکرد                                   | کلیدهای میانبر     |
| ---------------------------------------- | ------------------ |
| باز کردن ترمینال                         | `ctrl`+`alt`+`T`   |
| باز کردن تب جدید در ترمینال              | `ctrl`+`shift`+`T` |
| بستن ترمینال                             | `ctrl` + `D`       |
| توقف فرایند در حال اجرا، کپی             | `ctrl` + `C`       |
| تعلیق فرایند در حال اجرا، بازگشت تغییرات | `ctrl` + `Z`       |
| ذخیره کردن                               | `ctrl` + `S`       |
| مشاهده لیست تاریخچه دستورات              | `ctrl` + `R`       |

‌
###
---
>>###  چاپ پیام
>###### ‌دستور echo

```bash
echo "Hello World"
```
```sh
echo $(1+2)
```
```d
$ Hello World
```
```d
$ 3
```
‌
‌
---
---
ساختار فرضی پوشه های سیستم:
```r
/
└──home/
    └── ​user/
      │
      ├── myfiles/
      │      ├── video/
      │      │     └──2024/
      │      │     └──2025/
      │      └── music/
      │            └──pop/
      │            │   └── trk.mp3 
      │            └──hip-hop/
      │
      ├── mycode/
      │     ├── temp/
      │     └── python/
      │           └── app.py
      └── system.log
```
‌
>> ### مشاهده مسیر دایرکتوری فعلی 
>###### دستور `pwd` ‌
```bash
pwd
```
‌
#### 
---
> >### تغییر مسیر ‌
> ###### ‌دستور `cd`


`مسیر فرضی فعلی:`
 `/home/user/myfiles/video/`
###### • جابجایی به پوشه بعدی 
```bash
cd [نام پوشه]
```
```sh
pwd
```
```d
$ /user/myfiles/video/2024
```
‌
##### • جابجایی به پوشه‌ای در خارج از مسیر فعلی:
```sh
cd [مسیر پوشه]
```
‌
```sh
cd user/myfiles/music/pop
```
```sh
pwd
```
```d
$ user/myfiles/music/pop
```
‌‌
‌
`در لینوکس` `( .. )` `به معنی پوشه پدر و` `( . )` `به معنی پوشه فعلیه.`
##### • جابجایی به یک پوشه قبل
```bash
cd ..
```
##### • جابجایی به دو پوشه قبل
```sh
cd ../..
```

##### • برگشت به اول مسیر
```sh
cd
```
####
---
---
>> ### ساختار کلی دستورات 
```bash
COMMAND [OPTIONS] [ARGUMENTS]
```
```sh
مثال: rm -rf home/user/system.log
```

  #### • بخش `COMMAND`
`دستوری که به سیستم داده میشه.`
##### • بخش `OPTION`
`آپشن‌ها شرایطی رو برای دستور مشخص میکنن تا به شکل های مختلفی اجرا بشه. معمولا با یک یا دو خط تیره مشخص میشن`
 ##### • بخش `ARGUMENT`
`ورودی‌های مورد نیاز برای COMMAND. ممکنه یک مسیر یا یک فایل باشه که دستور قراره روی اون اعمال بشه.`
‌
‌
اگه فایل یا پوشه در پوشه فعلی وجود داشت فقط نام اون رو در ادامه دستور وارد میکنیم. 
اگه فایل/پوشه موردنظر در خارج از مسیر فعلی بود باید اونو به طور کامل مسیر دهی کنیم.
برای مثال مسیر فایل `trk1.mp3` در سیستم ما:
`/home/user/myfiles/music/pop/trk1.mp3`
سیستم توسط این آدرس، فایل `trk1.mp3` رو پیدا کرده و به عنوان ورودی دستور قرار میده. 
‌
###
---
---
>> ### مشاهده لیست محتویات پوشه 
>###### ‌دستور `ls`

‌
##### • مشاهده محتویات پوشه‌ی فعلی:
```bash
ls
```
‌‌
  ##### • مشاهده محتویات یک مسیر مشخص:
```bash
ls /home/user/myfiles/video
```
```sh
$ 2024 2025
```
| کاربرد | آپشن |
| --------------- | ------------------------------------------------ |
| لیست دایرکتوری‌ها بر حسب آخرین زمان ویرایش |  `ls -t`  |
| لیست اطلاعات کامل دایرکتوری‌ها |  `ls -l`  |
| لیست تمام دایرکتوری‌ها حتی مخفی‌|   `ls -a`  |
| لیست دایرکتوری‌ها به صورت معکوس |   `ls -r`  |
| لیست دایرکتوری‌ها به صورت درختی |   `ls -R` |
| نمایش سایز فایل‌ها با MB, KB, GB |  `ls -lh` |

‌
###
---
---
>> ### ایجاد پوشه 
>###### ‌‌ دستور `mkdir`
##### • ایجاد پوشه در پوشه‌فعلی
```bash
mkdir [نام پوشه]
```
‌
##### • ایجاد پوشه در مسیر مشخص
```bash
mkdir [مسیر ایجاد پوشه]
```
```sh
mkdir /home/user/new-folder
```
‌

##### ‌‌ • ساخت همزمان چند پوشه:
```bash
mkdir fold1 fold2 fold3
```
##### • ایجاد پوشه با اسامیِ فاصله دار:
 مثلا `fold 1`
```bash
mkdir "fold 1" "fold 2"
```
```bash
mkdir fold\ 1 fold\ 2
```

| کاربرد                          | آپشن‌های `mkdir` |
| ------------------------------- | ---------------- |
| ساخت یک شاخه دایرکتوری تو در تو | `mkdir -p`       |

```bash
mkdir -p /home/myfiles2/Img/family
```
این مسیر از home به بعد وجود نداشت و با این آپشن به طور کامل با زیر پوشه ها ساخته شد.
```sh
ls -R /home/user/myfiles2
```
```d
‌ Img/
‌‌  └──family/
```
####
---
---
> >### ایجاد فایل خالی 
>###### ‌  دستور `touch`

##### • در پوشه فعلی
```bash
touch [نام فایل]
```
##### • در مسیر مشخص 
```sh
touch [مسیر فایل]
```
##### 
---
---
> > ### خواندن محتویات فایل متنی
> ###### دستور `cat`

##### •  در پوشه فعلی:
```bash
cat [نام فایل]
```
##### • در مسیر مشخص: 
```sh
cat [مسیر فایل]
```
‌
####
---
---
>> ### کپی کردن 
> ###### ‌دستور `cp`
‌
##### • کپی کردن فایل موجود در پوشه فعلی به یک مسیر مشخص:
```sh
cp [نام فایل/پوشه] [مسیر کپی]
```
```bash
cp system.log home/user/mycode/temp/copy-sys.log
```
`فایل با نام جدید ذخیره و در صورت خالی گذاشتن ادامه مسیر، با نام خودش ذخیره میشه.`

| کاربرد                                                              | آپشن‌های `cp` |
| ------------------------------------------------------------------- | ------------- |
| کپی‌کردن کل محتویات و زیرپوشه های پوشه‌ای که خالی یا تک‌فایله نیست. | `cp -r`       |

```sh
cp -r [فایل/پوشه] [مسیر کپی]
```
####
---
---
> >### انتقال دادن
 >###### ‌ دستور `mv`
‌
##### • انتقال فایل موجود در پوشه فعلی به یک مسیر مشخص:
```bash
mv [نام فایل/پوشه] [مسیر انتقال]
```
```‌‌sh
mv system.log home/user/mycode/temp/copy-sys.log

```
##### • تغییر نام فایل با `mv`
```bash
mv [نام فایل] [نام جدید فایل]
```
```sh
mv system.log old-file.log
```
####
---
---
> >### حذف 
> ##### دستور `rm`

```bash
rm [OPTION] [فایل یا مسیر فایل]
```
`آپشن rf- یک آپشن رایج برای حذف بدون پرسش و اجباریه.`
##### • حذف پوشه
```sh
rm -rf "folder-name"
```
```sh
rm -rf /home/user/...
```
####
---
---
> >### پیداکردن کاربرد هر دستور
###### `1.` آپشن `help--`

```bash
[COMMAND] --help
```

###### `2.` دستور `info`

```bash
info [COMMAND]
```

###### `3.` دستور `man`

```bash
man [COMMAND]
```

: دستور `man` سه خروجی داره:
###### `1.` بخش `NAME`:
`معرفی و نام کامل دستور`
###### `2.` بخش `SYNOPSIS`:
`نحوه استفاده از دستور`
###### `3.` بخش `DESCRIPTION`:
`معرفی آپشن‌های دستور`
‌‌
####
---
---
>>#### تاریخچه دستورات 
> ###### دستور `history`
```bash
history
```
•`کلید های جهت یابی هم تاریخچه دستورات رو نشون میدن.`
  ‌  ‌• `↑` : `دستورات اخیر از آخرین به اولین`
 ‌‌   • `↓` : `بازگشت به آخرین دستورات` 
 ‌
####
---
---
>> ### مشاهده تاریخ در ترمینال 
>###### دستور `date`
```bash
date
```
```
$ Mon Jun 3 08:22:21 +0330 2024
```

---
 ##### • فرمت‌های مختلف برای دیدن تاریخ:

```bash
date "+%FORMAT"
```
| کاربرد | فرمت |
| ---------------------------------- | ----- |
| نمایش تاریخ به صورت `mm/dd/yy` | %D |
| نمایش روز ماه ( `01 - 31` ) | %d |
| نمایش روز هفته به صورت کامل | %A |
| نمایش روز هفته به صورت خلاصه | %a |
| نمایش نام ماه‌میلادی به صورت خلاصه | %h %b |
| نمایش نام ماه‌میلادی به صورت کامل | %B |
| نمایش عددی ماه میلادی ( `01 - 12` ) | %m |
| نمایش دو رقم‌آخر سال میلادی | %y |
| نمایش سال میلادی به صورت کامل | %Y |
| نمایش ساعت به صورت `HH:MM:SS` | %T |
| نمایش ساعت | %H |
| نمایش دقیقه | %M |
| نمایش ثانیه | %S |
```bash
date +%D
```
```text
$ 06/09/25
```
```bash
date +%D-%H-%M-%S
```
```
$ 06/09/25-12-05-04
```
 #####  • `آپشن date--` :
`دیدن مقاطع زمانی مختلف`
```bash
date --date="Time-Zone"
```
`دیدن تاریخ پنج ثانیه قبل`
```bash
date --date="5 seconds ago"
```
`دیدن تاریخ چهار سال قبل`
```bash
date --date="4 years ago"
```
`دیدن تاریخ روز مشخصی از هفته`
```bash
date --date="monday"
```

#### •` آپشن set--`:
`تغییر تاریخ و ساعت سیستم`
```bash
date --set="Mon Nov 23 18:23:16 PDT 2024"
```
‌
####
---
---
> > ###  تقویم
> >  ###### دستور `cal`
```bash
cal
```
| کاربرد | آپشن |
| -------------------------- | -------------------------- |
| نمایش تقویم کامل سال | `cal -Y`  |
| نمایش تقویم n ماه بعد | `cal -A n`  |
| نمایش تقویم n ماه قبل | `cal -B n`   |
| نمایش تقویم سال و ماه مشخص | `cal` < yy >  |

---
